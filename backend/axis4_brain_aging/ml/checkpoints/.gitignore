# This trained checkpoint ships with the repo so the demo works out of the box.
# Any other .pth dropped here (e.g. alternate/experimental checkpoints) stays ignored.
*.pth
!best_ref_b_dropout03_lr5e5.pth
